{{!-- List all posts --}}
{{#each posts as |post|}}
  <div class="post">
    <h2>{{post.title}}</h2>
    <p>{{post.content}}</p>
    <p>Posted on {{format_date post.createdAt}}</p>
    <a href="/post/{{post.id}}">Read more</a>
  </div>
{{/each}}
